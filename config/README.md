# Config (VPS-sensitive)

`pins.json` and `PINS-ISSUED.txt` live on the VPS only (`/home/opc/StepDaddyLiveHD/config/`).
Do **not** rsync `--delete` this directory from a workstation that lacks those files.
